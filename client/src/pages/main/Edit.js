import React, { Fragment } from "react";

import { Nav } from "../../components/layout";
import EditFieldsContainer from "../../components/containers/EditFieldsContainer";

const Create = () => {
	return (
		<Fragment>
			<Nav />
			<EditFieldsContainer />
		</Fragment>
	);
};

export default Create;
